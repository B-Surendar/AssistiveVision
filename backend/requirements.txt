# ── Core ─────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9

# ── Vision ────────────────────────────────────────────────────────────────────
# Use headless build — no GUI dep (important on Windows Server / headless)
opencv-python-headless==4.9.0.80
numpy==1.26.4

# ── Object Detection ──────────────────────────────────────────────────────────
# YOLOv8n.pt (~6 MB) downloads automatically on first run
ultralytics==8.2.18

# ── Object Tracking ───────────────────────────────────────────────────────────
deep-sort-realtime==1.3.2

# ── OCR ───────────────────────────────────────────────────────────────────────
easyocr==1.7.1

# ── PyTorch (CPU-only — MUCH smaller download, ~250 MB vs 2 GB) ───────────────
# DeepSORT + EasyOCR both need torch; Gemma runs in Ollama so no torch-LLM needed
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
torchvision==0.18.0+cpu

# ── Evaluation / charting ─────────────────────────────────────────────────────
matplotlib==3.9.0
pandas==2.2.2
scipy==1.13.0
